name: Teste de Desempenho - TeaStore v15

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  performance:
    runs-on: ubuntu-latest

    steps:
      # ======================================================
      # 1. Checkout do repositório
      # ======================================================
      - name: Checkout do repositório
        uses: actions/checkout@v4

      # ======================================================
      # 2. Instalar dependências
      # ======================================================
      - name: Instalar dependências
        run: |
          sudo apt update -y
          sudo apt install -y default-jdk python3 python3-pip unzip wget curl
          pip install matplotlib pandas reportlab

          # ✅ Instalar última versão do k6 (corrigida)
          K6_VERSION=$(curl -s https://api.github.com/repos/grafana/k6/releases/latest | grep tag_name | cut -d '"' -f 4)
          echo "🔹 Instalando K6 versão: $K6_VERSION"
          wget https://github.com/grafana/k6/releases/download/${K6_VERSION}/k6-${K6_VERSION}-linux-amd64.tar.gz
          tar -xvzf k6-${K6_VERSION}-linux-amd64.tar.gz
          sudo mv k6-${K6_VERSION}-linux-amd64/k6 /usr/local/bin/

          # ✅ Instalar JMeter versão 5.4.3 (compatível com seus .jmx)
          wget https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-5.4.3.zip
          unzip -q apache-jmeter-5.4.3.zip
          sudo mv apache-jmeter-5.4.3 /opt/jmeter

          # ✅ Instalar Locust
          pip install locust

      # ======================================================
      # 3. Executar K6 com Web Dashboard
      # ======================================================
      - name: Executar teste simples (K6) com Web Dashboard
        run: |
          mkdir -p k6-reports
          echo "🌐 Iniciando K6 Web Dashboard (teste simples)..."
          nohup k6 run --out web-dashboard ./k6-teastore/teste-carga-simples.js \
            --summary-export=k6-reports/simple.json > k6-reports/simple.log 2>&1 &
          sleep 10
          echo "✅ K6 Web Dashboard ativo (modo headless)."

      - name: Executar teste complexo (K6) com Web Dashboard
        run: |
          mkdir -p k6-reports
          echo "🌐 Executando teste complexo com K6 Web Dashboard..."
          nohup k6 run --out web-dashboard ./k6-teastore/teste-carga-complexo.js \
            --summary-export=k6-reports/complex.json > k6-reports/complex.log 2>&1 &
          sleep 15

      # ======================================================
      # 4. Executar JMeter (Simples + Complexo com dashboards HTML)
      # ======================================================
      - name: Executar testes JMeter (com dashboards)
        run: |
          mkdir -p jmeter-reports

          SIMPLE="./jmeter-teastore/teste-carga-simples.jmx"
          COMPLEX="./jmeter-teastore/teste-carga-complexo.jmx"

          echo "🧪 Executando JMeter - Teste Simples..."
          /opt/jmeter/bin/jmeter -n -t "$SIMPLE" -l jmeter-reports/simple.jtl -e -o jmeter-reports/dashboard-simple

          echo "🧪 Executando JMeter - Teste Complexo..."
          /opt/jmeter/bin/jmeter -n -t "$COMPLEX" -l jmeter-reports/complex.jtl -e -o jmeter-reports/dashboard-complex

          echo "✅ Dashboards HTML gerados com sucesso!"

      # ======================================================
      # 5. Executar testes Locust (mantido sem alterações)
      # ======================================================
      - name: Executar testes Locust
        run: |
          mkdir -p locust-reports
          locust -f ./locust-teastore/locustfile.py \
            --headless -u 10 -r 2 -t 30s --csv=locust-reports/locust

      # ======================================================
      # 6. Gerar Dashboard Consolidado (HTML + PDF)
      # ======================================================
      - name: Gerar Dashboard Consolidado
        run: |
          mkdir -p reports
          echo "📊 Gerando dashboard consolidado..."
          python3 generate_dashboard.py \
            --k6 k6-reports/complex.json \
            --jmeter jmeter-reports/complex.jtl \
            --locust locust-reports/locust_stats.csv \
            --out reports/dashboard.html \
            --pdf reports/dashboard.pdf

      # ======================================================
      # 7. Upload dos Relatórios
      # ======================================================
      - name: Upload dos Relatórios
        uses: actions/upload-artifact@v4
        with:
          name: relatorios-desempenho-v15
          path: |
            k6-reports/
            jmeter-reports/
            locust-reports/
            reports/
