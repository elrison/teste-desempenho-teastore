name: Pipeline de Performance (v28 - Brute Force Wait)

on:
  workflow_dispatch:

jobs:
  performance:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose netcat-openbsd curl wget python3-pip python3-bs4

      # -----------------------------------------------------------------
      # CORREÇÃO CRÍTICA: Aumentar SLEEPs para 2 min (backend) e 1 min (frontend)
      # -----------------------------------------------------------------
      - name: Start TeaStore stack (Manually Ordered)
        run: |
          # FASE 1: O BANCO DE DADOS
          echo "🚀 FASE 1: Iniciando SOMENTE o Banco de Dados (teastore-db)..."
          docker compose up -d teastore-db
          
          echo "⏳ Aguardando o Banco de Dados ficar 'healthy' (max 2 min)..."
          MAX_WAIT=120
          COUNT=0
          while [ $COUNT -lt $MAX_WAIT ]; do
            HEALTH=$(docker ps --filter "name=teastore-db-1" --format "{{.Status}}")
            if echo "$HEALTH" | grep -q "(healthy)"; then
              echo "✅ DB está 'healthy'!"
              break
            fi
            if echo "$HEALTH" | grep -q "unhealthy"; then
              echo "❌ DB está 'unhealthy'. Falha imediata."
              docker logs teastore-db-1
              exit 1
            fi
            echo "   (Aguardando $COUNT/$MAX_WAIT... Status: $HEALTH)"
            sleep 2
            COUNT=$((COUNT + 2))
          done
          if [ $COUNT -ge $MAX_WAIT ]; then
            echo "❌ Timeout: O Banco de Dados não ficou 'healthy' a tempo."
            docker logs teastore-db-1
            exit 1
          fi

          # FASE 2: O REGISTRY (depende do DB)
          echo "🚀 FASE 2: Iniciando o Registry..."
          docker compose up -d teastore-registry
          echo "⏳ Aguardando 20s para o Registry se conectar ao DB..."
          sleep 20
          
          # FASE 3: O BACKEND (depende do Registry e DB)
          echo "🚀 FASE 3: Iniciando os serviços de Backend (Persistence, Auth, etc.)..."
          docker compose up -d teastore-persistence teastore-auth teastore-image teastore-recommender
          echo "⏳ Aguardando 120 SEGUNDOS (2 Min) para os .war do backend implantarem..."
          # AUMENTADO DE 90 para 120
          sleep 120

          # FASE 4: O FRONTEND (depende do Registry)
          echo "🚀 FASE 4: Iniciando o Frontend (WebUI)..."
          docker compose up -d teastore-webui
          echo "⏳ Aguardando 60 SEGUNDOS (1 Min) para o .war do WebUI implantar..."
          # AUMENTADO DE 45 para 60
          sleep 60
          
          echo "✅ Todos os serviços estão iniciados em ordem."

      # -----------------------------------------------------------------
      # Este passo (v26) agora tem o tempo necessário para funcionar
      # -----------------------------------------------------------------
      - name: Populate & Wait for Products (v26 logic)
        run: |
          GEN_URL="http://localhost:18081/tools.descartes.teastore.webui/services/rest/persistence/generateDatabase?products=500&clients=20&categories=50&orders=20"
          HOME_URL="http://localhost:18081/tools.descartes.teastore.webui/"
          
          echo "🚀 Iniciando Loop de Verificação da API de Persistência (max 2 min)..."
          
          # --- LOOP 1: Esperar a API de Geração ficar ONLINE ---
          MAX_WAIT_API=120 # 2 minutos
          COUNT=0
          API_READY=false
          while [ $COUNT -lt $MAX_WAIT_API ]; do
            echo "--- Tentativa API (Tempo $COUNT/$MAX_WAIT_API) ---"
            
            # -s (silencioso)
            # -o /dev/null (descarta o body)
            # -w "%{http_code}" (imprime o status code)
            # *** SEM -L ***
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" -X POST "$GEN_URL")

            if [ "$HTTP_CODE" == "200" ] || [ "$HTTP_CODE" == "204" ]; then
              echo "✅ API de Geração respondeu com $HTTP_CODE! O Backend está online."
              API_READY=true
              break
            fi
            
            echo "   API de Persistência ainda não está pronta (HTTP $HTTP_CODE). Aguardando..."
            sleep 5
            COUNT=$((COUNT + 5))
          done

          if [ "$API_READY" == "false" ]; then
            echo "❌ Erro Fatal: A API de geração de dados nunca respondeu com 200/204."
            docker compose logs teastore-persistence
            exit 1
          fi

          # --- LOOP 2: Esperar os DADOS aparecerem (são gerados async) ---
          
          echo "⏳ API online. Aguardando dados aparecerem na Home Page (max 60s)..."
          MAX_WAIT_DATA=60
          COUNT=0
          while [ $COUNT -lt $MAX_WAIT_DATA ]; do
            HOME_HTML=$(curl -L -s "$HOME_URL")
            CAT_LINK=$(echo "$HOME_HTML" | grep -o 'category?id=[0-9]*' | head -n 1)

            if [ -n "$CAT_LINK" ]; then
               FULL_URL="http://localhost:18081/tools.descartes.teastore.webui/$CAT_LINK"
               RESP=$(curl -L -s --max-time 5 "$FULL_URL" || true)

               if [ -n "$RESP" ] && echo "$RESP" | grep -q "product?id="; then
                 echo "✅ SUCESSO! Produtos encontrados em $FULL_URL."
                 exit 0
               fi
               echo "   Categorias encontradas, mas ainda sem produtos. Aguardando..."
            else
               echo "   Home page ainda sem categorias. Aguardando..."
            fi
            
            sleep 5
            COUNT=$((COUNT + 5))
          done

          echo "❌ Erro Fatal: O loop terminou e os produtos nunca apareceram."
          echo "--- Debug: Logs Atuais da Persistência ---"
          docker compose logs teastore-persistence || true
          exit 1

      - name: Check containers
        run: docker ps -a

      # -----------------------
      # JMETER (sem alterações)
      # -----------------------
      - name: Install Java & JMeter
        run: |
          sudo apt-get install -y openjdk-11-jre-headless
          wget -q https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-5.6.2.tgz
          tar -xzf apache-jmeter-5.6.2.tgz

      - name: Run JMeter Simples
        run: |
          ./apache-jmeter-5.6.2/bin/jmeter -n \
            -t jmeter-teastore/teste-carga-simples.jmx \
            -l jmeter-teastore/results-simples.jtl \
            -e -o jmeter-teastore/report-simples \
            -Jhostname=localhost -Jport=18081 || true

      - name: Run JMeter Complexos
        run: |
          ./apache-jmeter-5.6.2/bin/jmeter -n \
            -t jmeter-teastore/cenarios-complexos.jmx \
            -l jmeter-teastore/results-complexos.jtl \
            -e -o jmeter-teastore/report-complexos \
            -Jhostname=localhost -Jport=18081 || true

      - name: Upload JMeter reports
        uses: actions/upload-artifact@v4
        with:
          name: relatorios-jmeter
          path: jmeter-teastore/report*

      # -----------------------
      # K6 (sem alterações)
      # -----------------------
      - name: Install K6
        run: |
          curl -s https://dl.k6.io/key.gpg | sudo apt-key add -
          echo "deb https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install -y k6

      - name: Run K6 Simples
        run: |
          k6 run k6-teastore/teste-carga-simples.js --summary-export=k6-simple.json || true

      - name: Run K6 Complexos
        run: |
          k6 run k6-teastore/cenarios-complexos.js || true

      - name: Upload K6 summaries
        uses: actions/upload-artifact@v4
        with:
          name: relatorios-k6
          path: |
            k6-*.json
            k6*.html

      # -----------------------
      # LOCUST (sem alterações)
      # -----------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install locust beautifulsoup4 requests pandas matplotlib lxml reportlab

      - name: Run Locust Simples
        run: |
          locust -f locust-teastore/locustfile.py --headless -u 5 -r 2 -t 15s \
            --host http://localhost:18081 \
            --html locust-teastore/simple.html \
            --csv locust-teastore/simple || true

      - name: Run Locust Complexos
        run: |
          locust -f locust-teastore/cenarios-complexos-locust.py --headless -u 10 -r 3 -t 30s \
            --host http://localhost:18081 \
            --html locust-teastore/complex.html \
            --csv locust-teastore/complex || true

      - name: Upload Locust HTML
        uses: actions/upload-artifact@v4
        with:
          name: relatorios-locust
          path: locust-teastore/*.html

      # -----------------------
      # CONSOLIDATED (sem alterações)
      # -----------------------
      - name: Generate consolidated dashboard & PDF
        run: |
          python3 generate_dashboard.py \
            --k6 k6-complex.json \
            --jmeter jmeter-teastore/results-complexos.jtl \
            --locust locust-teastore/complex_stats.csv \
            --pdf report.pdf \
            --out dashboard.html || true

      - name: Upload consolidated
        uses: actions/upload-artifact@v4
        with:
          name: relatorio-consolidado
          path: |
            dashboard.html
            report.pdf
            charts/
        if: always()

      - name: Logs & Stop containers
        if: always()
        run: |
          docker compose logs --no-color
          docker compose down