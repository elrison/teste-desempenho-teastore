name: Pipeline de Performance (v21 - Root Directory Fix)

on:
  workflow_dispatch:

jobs:
  performance:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    # NÃO HÁ 'DEFAULTS' DE DIRETÓRIO AQUI - TUDO RODA NA RAIZ
    
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        # O 'path:' foi removido. O código fica na raiz.

      - name: Install system packages
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose netcat-openbsd curl wget python3-pip python3-bs4
        # Não precisa de 'working-directory'

      - name: Start TeaStore stack
        run: |
          echo "🚀 Iniciando Banco de Dados..."
          # Encontra o docker-compose.yml na raiz
          docker compose up -d teastore-db
          
          echo "⏳ Aguardando 40s para o MySQL inicializar..."
          sleep 40
          
          echo "🚀 Iniciando Registry..."
          docker compose up -d teastore-registry
          sleep 20
          
          echo "🚀 Iniciando restante da Stack..."
          docker compose up -d teastore-persistence teastore-auth teastore-image teastore-recommender
          sleep 20
          docker compose up -d teastore-webui
          sleep 15
        # Não precisa de 'working-directory'

      - name: Wait WebUI (Login Page)
        run: |
          MAX_ATTEMPTS=80
          SLEEP_SECONDS=3
          URL="http://localhost:18081/tools.descartes.teastore.webui/login"

          echo "⏳ Aguardando WebUI na URL: $URL (até $((MAX_ATTEMPTS * SLEEP_SECONDS))s)..."

          for attempt in $(seq 1 $MAX_ATTEMPTS); do
            RESP=$(curl -L -s --max-time 5 "$URL" || true)
            if [ -n "$RESP" ]; then
              if echo "$RESP" | grep -qi "TeaStore"; then
                echo "✅ Página TeaStore carregada na tentativa $attempt"
                exit 0
              fi
            fi
            sleep $SLEEP_SECONDS
          done
          echo "❌ Timeout: WebUI não carregou."
          exit 1
        # Não precisa de 'working-directory'

      - name: Fix Connection & Populate
        run: |
          echo "🔧 Reiniciando container de Persistência para forçar reconexão com DB..."
          # Encontra o docker-compose.yml na raiz
          docker compose restart teastore-persistence
          
          echo "⏳ Aguardando 15s para Persistência subir novamente..."
          sleep 15

          # 1. FORÇAR GERAÇÃO DE DADOS
          GEN_URL="http://localhost:18081/tools.descartes.teastore.webui/services/rest/persistence/generateDatabase?products=500&clients=20&categories=50&orders=20"
          
          echo "🚀 Forçando geração de dados via: $GEN_URL"
          curl -v -X POST "$GEN_URL" || true
          
          echo "⏳ Aguardando 15s para gravação dos dados..."
          sleep 15

          # 2. DESCOBRIR UMA CATEGORIA VÁLIDA
          echo "🔍 Buscando uma categoria válida na Home Page..."
          HOME_HTML=$(curl -s "http://localhost:18081/tools.descartes.teastore.webui/")
          CAT_LINK=$(echo "$HOME_HTML" | grep -o 'category?id=[0-9]*' | head -n 1)

          if [ -z "$CAT_LINK" ]; then
             echo "⚠️ Nenhuma categoria encontrada. Fallback para ID=2..."
             CAT_LINK="category?id=2"
          else
             echo "✅ Categoria detectada: $CAT_LINK"
          fi

          # 3. VERIFICAR SE OS PRODUTOS APARECERAM
          MAX_ATTEMPTS=60
          SLEEP_SECONDS=2
          FULL_URL="http://localhost:18081/tools.descartes.teastore.webui/$CAT_LINK"
          
          echo "⏳ Verificando produtos em $FULL_URL..."

          for attempt in $(seq 1 $MAX_ATTEMPTS); do
            RESP=$(curl -L -s --max-time 5 "$FULL_URL" || true)
            if [ -n "$RESP" ]; then
              if echo "$RESP" | grep -q "product?id="; then
                echo "✅ Produtos encontrados na tentativa $attempt! Sucesso."
                exit 0
              fi
            fi
            sleep $SLEEP_SECONDS
          done

          echo "❌ Erro Fatal: Banco de dados vazio."
          echo "--- Debug: Logs Atuais da Persistência ---"
          # O nome do container agora será baseado no nome do diretório raiz
          docker compose logs teastore-persistence || true
          exit 1
        # Não precisa de 'working-directory'

      - name: Check containers
        run: docker ps -a
        # Não precisa de 'working-directory'

      # -----------------------
      # JMETER
      # -----------------------
      - name: Install Java & JMeter
        run: |
          sudo apt-get install -y openjdk-11-jre-headless
          wget -q https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-5.6.2.tgz
          tar -xzf apache-jmeter-5.6.2.tgz

      - name: Run JMeter Simples
        run: |
          ./apache-jmeter-5.6.2/bin/jmeter -n \
            -t jmeter-teastore/teste-carga-simples.jmx \
            -l jmeter-teastore/results-simples.jtl \
            -e -o jmeter-teastore/report-simples \
            -Jhostname=localhost -Jport=18081 || true

      - name: Run JMeter Complexos
        run: |
          ./apache-jmeter-5.6.2/bin/jmeter -n \
            -t jmeter-teastore/cenarios-complexos.jmx \
            -l jmeter-teastore/results-complexos.jtl \
            -e -o jmeter-teastore/report-complexos \
            -Jhostname=localhost -Jport=18081 || true

      - name: Upload JMeter reports
        uses: actions/upload-artifact@v4
        with:
          name: relatorios-jmeter
          # CORREÇÃO: Removido prefixo do path
          path: jmeter-teastore/report*

      # -----------------------
      # K6
      # -----------------------
      - name: Install K6
        run: |
          curl -s https://dl.k6.io/key.gpg | sudo apt-key add -
          echo "deb https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install -y k6

      - name: Run K6 Simples
        run: |
          k6 run k6-teastore/teste-carga-simples.js --summary-export=k6-simple.json || true

      - name: Run K6 Complexos
        run: |
          k6 run k6-teastore/cenarios-complexos.js || true

      - name: Upload K6 summaries
        uses: actions/upload-artifact@v4
        with:
          name: relatorios-k6
          # CORREÇÃO: Removido prefixo do path
          path: |
            k6-*.json
            k6-*.html

      # -----------------------
      # LOCUST
      # -----------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install locust beautifulsoup4 requests pandas matplotlib lxml reportlab

      - name: Run Locust Simples
        run: |
          locust -f locust-teastore/locustfile.py --headless -u 5 -r 2 -t 15s \
            --host http://localhost:18081 \
            --html locust-teastore/simple.html \
            --csv locust-teastore/simple || true

      - name: Run Locust Complexos
        run: |
          locust -f locust-teastore/cenarios-complexos-locust.py --headless -u 10 -r 3 -t 30s \
            --host http://localhost:18081 \
            --html locust-teastore/complex.html \
            --csv locust-teastore/complex || true

      - name: Upload Locust HTML
        uses: actions/upload-artifact@v4
        with:
          name: relatorios-locust
          # CORREÇÃO: Removido prefixo do path
          path: locust-teastore/*.html

      # -----------------------
      # CONSOLIDATED
      # -----------------------
      - name: Generate consolidated dashboard & PDF
        run: |
          python3 generate_dashboard.py \
            --k6 k6-complex.json \
            --jmeter jmeter-teastore/results-complexos.jtl \
            --locust locust-teastore/complex_stats.csv \
            --pdf report.pdf \
            --out dashboard.html || true

      - name: Upload consolidated
        uses: actions/upload-artifact@v4
        with:
          name: relatorio-consolidado
          # CORREÇÃO: Removido prefixo do path
          path: |
            dashboard.html
            report.pdf
            charts/
        if: always()

      - name: Logs & Stop containers
        if: always()
        run: |
          docker compose logs --no-color
          docker compose down